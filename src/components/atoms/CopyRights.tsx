import * as React from "react";
import styled from "styled-components";

const StyledCopyRights = styled.div`
  color: #696969;
  font-size: 0.78em;
`;

const CopyRights = () => <StyledCopyRights>© 2018 Mockbiz</StyledCopyRights>;

export default CopyRights;
